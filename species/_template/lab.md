# Lab

## 目的

用最小、可重复、隔离的实验验证一个协议机制。

## 前置条件

- Linux、`ip`、`tcpdump`；
- 运行 `scripts/lab-netns.sh setup`；
- 不连接公网。

## 步骤

待填写：服务端、客户端、capture filter、预期 frame。

## 结果

将机器可读结果和 capture 放在 `captures/`，并按 `docs/CAPTURE-CONVENTION.md` 记录元数据。
