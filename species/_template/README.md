# Protocol species: `<name>`

## 展签

- **历史用途**：
- **定义层次**：
- **出生语境 (`birth_context`)**：
- **当前状态**：
- **证据等级 (`evidence_level`)**：`real-capture` / `fixture` / `static` / `document-reconstruction` / `not-run`
- **现代替代/存续原因**：

## 隐含假设

- **线缆/拓扑 (`topology_assumption`)**：
- **寻址 (`addressing_model`)**：
- **发现 (`discovery_model`)**：
- **信任 (`trust_model`)**：
- **状态 (`state_model`)**：
- **失败 (`failure_model`)**：
- **带宽/算力 (`bandwidth_assumption`)**：
- **中间盒 (`middlebox_assumption`)**：
- **死亡压力 (`death_pressure`)**：
- **幸存后代 (`surviving_descendants`)**：

## 既有实现

记录复用的成熟 server/client、版本和平台。不要为了本展品重写完整生产实现。

## 实验入口

见 `lab.md`。所有明文或弱认证实验默认只在 loopback/netns 中运行。

## 资料

见 `sources.md`，并注明实验与历史推断的证据边界。
