# talk / talkd 展品

署名：**祀（岁家老十三）**。

历史 BSD talk 将“邀请”与“对话数据”分开：talk 客户端向 talkd 注册/查询目标用户终端，daemon 通知对方并返回 rendezvous 信息，双方随后建立终端会话。它依赖 Unix 登录用户、TTY、主机名和权限模型，而非现代账号/联系人系统。

现代 Linux 通常缺少完整 talkd/可用 TTY，因此本仓只做规范与实现分析及可复核状态图，不宣称运行成功。风险包括伪造邀请、终端注入、主机信任和明文内容；实验不得接触真实登录会话。
